import 'package:flutter/material.dart';

class RequestViewWidget extends StatelessWidget {
  const RequestViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
