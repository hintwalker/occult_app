import 'package:flutter/material.dart';

class StarListWidget extends StatelessWidget {
  const StarListWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
