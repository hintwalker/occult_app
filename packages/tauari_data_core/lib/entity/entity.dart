part of tauari_data_core;

abstract class Entity implements Dumpable {
  const Entity();
  // Map<String, Object?> toCloud();
  // T copyWithOnCloud(String? value);
  // T copyWithState(String? state);

  // final String? state;
  // final int id;
  // String get keyId => this.id.toString();
  // @override
  // bool operator ==(Object? other) {
  //   if (other is Entity) {
  //     return keyId == other.keyId;
  //   }
  //   return false;
  // }

  // @override
  // int get hashCode => id.hashCode;
}
